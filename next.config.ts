import type { NextConfig } from "next";

// I keep my main Next.js settings in this file.
const nextConfig: NextConfig = {
  // I turn on the React Compiler to help optimize the app automatically.
  reactCompiler: true,
};

export default nextConfig;
